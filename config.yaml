data:
  raw_path: "data/raw/*.csv"
  processed_path: "data/processed/"
  
pipeline:
  test_size: 0.2
  random_state: 42
  cv_folds: 5
  
models:
  logistic: {max_iter: 1000}
  rf: {n_estimators: 100}
  xgb: {eval_metric: 'logloss'}
